  <header>
    <nav class="navbar navbar-default">
      <div class="container">
        <div class="navbar-header">
          <button class="navbar-toggle collapsed" type="button" data-toggle="collapse" data-target="#main-navbar-collapse" aria-expanded="false">
            <span class="sr-only">Toggle navigation</span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
          </button>
          <%= link_to 'READING NOW', root_path, id: 'logo', class: 'navbar-brand'%>
        </div>
        <div class="collapse navbar-collapse" id="main-navbar-collapse">
          <ul class="nav navbar-nav">
            <%= menu_li_tag('#') do %>
              <span class="fa fa-book"></span> <span>在读</span>
            <% end %>
            <%= menu_li_tag('#') do %>
              <span class="fa fa-calendar"></span> <span>读迹</span>
            <% end %>
            <%= menu_li_tag('#') do %>
              <span class="fa fa-archive"></span> <span>书库</span>
            <% end %>
          </ul>
          <%#= form_tag '#', method: :get, class: 'navbar-form navbar-left' do %>
          <!-- <div class="form-group"> -->
          <!-- <input class="form-control search-input" name="w" type="text" value="<%= params[:w] %>" placeholder="搜索图书、笔记"/> -->
          <!-- </div> -->
          <%# end %>
          <% if user_signed_in? %>
            <ul class="nav navbar-nav navbar-right">
              <%= menu_li_tag('#') do %>
                <span class="fa fa-envelope"></span> <span>消息</span>
              <% end %>
              <li class="dropdown">
                <a class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false">
                  Hello, wang
                  <span class="caret"></span>
                </a>
                <ul class="dropdown-menu">
                  <%= menu_li_tag(edit_user_registration_path) do %>
                    <span class="fa fa-user"></span> 个人中心
                  <% end %>
                  <li class="divider" role="seperator"></li>
                  <%= menu_li_tag('#') do %>
                    <span class="fa fa-sign-out"></span> 登出
                  <% end %>
                </ul>
            </li>
            </ul>
          <% else %>
            <ul class="nav navbar-nav navbar-right">
              <li>
                <%= menu_li_tag(new_user_registration_path) do %>
                  <span class="fa fa-user-plus"></span> 注册
                <% end %>
              </li>
                <%= menu_li_tag(new_user_session_path) do %>
                  <span class="fa fa-sign-in"></span> 登录
                <% end %>
            </ul>
          <% end %>
        </div>
      </div>
    </nav>
  </header>
